<?xml version="1.0" encoding="utf-8" ?>
<app:BasePage
    x:Class="MAUIsland.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:app="clr-namespace:MAUIsland"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    Title="SettingsPage"
    x:DataType="app:SettingsPageViewModel">
    <VerticalStackLayout>
        <toolkit:AvatarView ImageSource="{x:Binding AvatarImageSource}" />
        <Button Command="{x:Binding OpenFileCommand}" Text="Upload image" />
        <Button
            Command="{x:Binding SaveCommand}"
            IsEnabled="{x:Binding AvatarImageSource,
                                  Converter={toolkit:IsNotNullConverter}}"
            Text="Save" />
    </VerticalStackLayout>
</app:BasePage>